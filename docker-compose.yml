version: '3.9'
services:
  azurite:
    image: mcr.microsoft.com/azure-storage/azurite
    container_name: "azurite"
    hostname: azurite
    restart: always
    #command: 'azurite --oauth basic --cert /workspace/azurite.com+5.pem --key /workspace/azurite.com+5-key.pem --location /workspace --debug /workspace/debug.log --blobHost 0.0.0.0  --queueHost 0.0.0.0 --tableHost 0.0.0.0  --loose'
    ports:
      - "10000:10000"
      - "10001:10001"
      - "10002:10002" 
    volumes:
      - ./azuriteData:/workspace